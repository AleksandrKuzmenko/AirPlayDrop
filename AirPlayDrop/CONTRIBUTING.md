# Contributing

Please open an issue describing the source container, codec and FFmpeg version
before large changes. Keep pull requests focused and include tests for pure
logic or state transitions.

Run the build, test, and `git diff --check` commands in the README. Never add
copyrighted media or personal media paths to fixtures, logs, screenshots, or
tests. Generated fixtures must be tiny and deterministic.
