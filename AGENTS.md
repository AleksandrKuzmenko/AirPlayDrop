# Development rules

Run the root README build/test commands. Tests use generated fixtures or mocks only and never access user media. Keep FFmpeg arguments structured, validate outputs before marking them ready, and do not add binaries, credentials, or private APIs.
